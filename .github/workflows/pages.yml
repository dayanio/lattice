name: Package Repo Index

# RPM/DEB packaging disabled until stable release
# Re-enable when nfpms section is restored in .goreleaser.yaml
on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Package repo indexing disabled (pending stable release)"
